# student-portal

A minimal student portal script to add students and persist their data locally.

## Usage

Run:

```bash
python students.py
```

Provide the student's name, age, and ID when prompted. Data is saved in `students.json`.
